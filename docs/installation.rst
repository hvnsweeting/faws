.. highlight:: shell

============
Installation
============


Stable release
--------------

To install faws, run this command in your terminal:

.. code-block:: console

    $ pip install faws

This is the preferred method to install faws, as it will always install the most recent stable release.

If you don't have `pip`_ installed, this `Python installation guide`_ can guide
you through the process.

.. _pip: https://pip.pypa.io
.. _Python installation guide: http://docs.python-guide.org/en/latest/starting/installation/


From sources
------------

The sources for faws can be downloaded from the `Github repo`_.

You can either clone the public repository:

.. code-block:: console

    $ git clone git://github.com/hvnsweeting/faws

Or download the `tarball`_:

.. code-block:: console

    $ curl  -OL https://github.com/hvnsweeting/faws/tarball/master

Once you have a copy of the source, you can install it with:

.. code-block:: console

    $ python setup.py install


.. _Github repo: https://github.com/hvnsweeting/faws
.. _tarball: https://github.com/hvnsweeting/faws/tarball/master
